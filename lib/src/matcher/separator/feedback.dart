import '../../options.dart';
import '../../types.dart';

Feedback? separatorFeedback({
  required MatchEstimated match,
  required Options options,
  bool? isSoleMatch,
}) {
  // No suggestions.
  return null;
}
